'use client'
import {useEffect,useState} from 'react'
import Link from 'next/link'
import {ArrowLeft,ArrowUpRight,Globe2,TrendingUp,BarChart3} from 'lucide-react'
function Card({s}){return <Link className="topicStory" href={`/story/${s.id}`}><div><span>{s.category}</span><b>{Math.round(Number(s.momentum_score||0))}</b></div><h3>{s.title}</h3><p>{s.sentiment||'neutral'} · {s.published_at?new Date(s.published_at).toLocaleDateString():''}</p><ArrowUpRight size={15}/></Link>}
export default function TopicPage({params}){const[topic,setTopic]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{fetch(`/api/topic/${params.slug}`).then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.error||'Topic unavailable');return d.topic}).then(setTopic).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[params.slug]);if(loading)return <main className="topicPage"><div className="inner pageState">Loading topic intelligence…</div></main>;if(error||!topic)return <main className="topicPage"><div className="inner pageState"><Link href="/" className="back"><ArrowLeft size={16}/> Back to Infera</Link><h1>Topic unavailable</h1><p>{error||'No intelligence has been collected for this topic yet.'}</p></div></main>;return <main className="topicPage"><header className="topbar"><div className="nav inner"><Link className="brand" href="/"><span>I</span> INFERA</Link><div className="navSpacer"/><Link href="/" className="back"><ArrowLeft size={15}/> Feed</Link></div></header><section className="topicHero inner"><div><div className="eyebrow"><Globe2 size={12}/> TOPIC INTELLIGENCE</div><h1>{topic.name}</h1><p>{topic.storyCount} stories tracked by Infera, aggregated into one evolving signal.</p></div><div className="topicSignal"><span>AVERAGE MOMENTUM</span><strong>{topic.avgMomentum}<small>/100</small></strong><div className="topicMeter"><i style={{width:`${topic.avgMomentum}%`}}/></div><em>{topic.avgMomentum>=85?'Accelerating':topic.avgMomentum>=70?'Rising':topic.avgMomentum>=45?'Monitoring':'Quiet'}</em></div></section><section className="inner topicStats"><div><BarChart3/><span>Momentum</span><b>{topic.avgMomentum}</b></div><div><TrendingUp/><span>Importance</span><b>{topic.avgImportance}</b></div><div><span>Market relevance</span><b>{topic.avgMarketRelevance}</b></div><div><span>Africa relevance</span><b>{topic.avgAfricaRelevance}</b></div></section><section className="inner topicFeed"><div className="eyebrow">EVOLVING COVERAGE</div><h2>Latest intelligence</h2><div className="topicGrid">{topic.stories.map(s=><Card key={s.id} s={s}/>)}</div></section></main>}
