name: Infera Intelligence Refresh

# Retained as a manual compatibility workflow only.
# Scheduled production ingestion is owned by infera-ingestion.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical ingestion workflow
        run: |
          echo "Scheduled ingestion is handled by Infera Intelligence Ingestion."
          echo "Run .github/workflows/infera-ingestion.yml for production refreshes."
