name: Legacy News Ingestion

# Retained as a manual compatibility workflow only.
# Production ingestion is owned by infera-ingestion.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical ingestion workflow
        run: |
          echo "The legacy Nexora ingestion workflow is disabled."
          echo "Scheduled production ingestion is handled by Infera Intelligence Ingestion."
