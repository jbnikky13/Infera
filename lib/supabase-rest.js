const baseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
function configured() { return Boolean(baseUrl && serviceKey); }
async function request(path, options = {}) { if (!configured()) return null; const response = await fetch(`${baseUrl}/rest/v1/${path}`, { ...options, headers: { apikey: serviceKey, Authorization: `Bearer ${serviceKey}`, 'Content-Type': 'application/json', Prefer: 'return=representation,resolution=merge-duplicates', ...(options.headers || {}) }, cache: 'no-store' }); if (!response.ok) throw new Error(`Supabase ${response.status}: ${await response.text()}`); return response.status === 204 ? null : response.json(); }
export async function persistStories(stories, sources) { if (!configured()) return { persisted:false,reason:'Supabase environment variables are not configured',count:0 }; let count=0; for(const source of sources){await request(`sources?name=eq.${encodeURIComponent(source.name)}`,{method:'POST',body:JSON.stringify({name:source.name,url:source.url,feed_url:source.url,category:source.category,active:true})})} for(const story of stories){const sourceRows=await request(`sources?name=eq.${encodeURIComponent(story.sourceName)}&select=id`);const sourceId=sourceRows?.[0]?.id||null;const rows=await request('stories',{method:'POST',body:JSON.stringify({source_id:sourceId,external_url:story.url,title:story.title,description:story.description||null,category:story.category,published_at:story.publishedAt,summary:story.summary||null,why_it_matters:story.whyItMatters||null,what_to_watch:story.whatToWatch||null,sentiment:story.sentiment||null,importance_score:story.importanceScore||0,momentum_score:story.momentumScore||0,market_relevance:story.marketRelevance||0,africa_relevance:story.africaRelevance||0,is_breaking:(story.importanceScore||0)>=90,ai_processed:Boolean(story.aiProcessed)} )}); const saved=rows?.[0]; if(saved?.id){await request('story_snapshots',{method:'POST',body:JSON.stringify({story_id:saved.id,momentum_score:story.momentumScore||0,importance_score:story.importanceScore||0})}); for(const entity of (story.entities||[])){const name=typeof entity==='string'?entity:entity.name;const type=typeof entity==='string'?'topic':(entity.type||'topic');if(name) await request('story_entities',{method:'POST',body:JSON.stringify({story_id:saved.id,name,entity_type:type})})}} count++} return {persisted:true,count}; }
export async function getStory(id){ if(!configured()) return null; const rows=await request(`stories?id=eq.${encodeURIComponent(id)}&select=*,sources(name,url)&limit=1`); const story=rows?.[0]; if(!story)return null; const entities=await request(`story_entities?story_id=eq.${encodeURIComponent(id)}&select=name,entity_type`); const snapshots=await request(`story_snapshots?story_id=eq.${encodeURIComponent(id)}&select=momentum_score,importance_score,captured_at&order=captured_at.asc`); return {...story,sourceName:story.sources?.name,sourceUrl:story.sources?.url,url:story.external_url,momentumScore:story.momentum_score,importanceScore:story.importance_score,marketRelevance:story.market_relevance,africaRelevance:story.africa_relevance,whyItMatters:story.why_it_matters,whatToWatch:story.what_to_watch,aiProcessed:story.ai_processed,entities:entities||[],snapshots:snapshots||[]}; }
